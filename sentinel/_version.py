"""Single source of truth for the running daemon's version string.

Bumped by `docs(release): cut [X.Y.Z]` PRs alongside CHANGELOG.md and
README.md's version badge. If you change the value here, also update
those two — drift means the GUI header lies about which build is
running.
"""
__version__ = "0.7.5"
